peer:
  # TLS disabled
  tls:
    enabled: false
  # Do NOT set mspConfigPath here—use CORE_PEER_MSPCONFIGPATH instead.
  BCCSP:
    Default: SW
    SW:
      Hash: SHA2
      Security: 256
      FileKeystore:
        KeyStore: keyStore
